name: Demo Workflow

on: 
  push:
    branches:
    - main
    paths:
      - "app.txt"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - run: echo "Hello! Git Hub actions."
  
