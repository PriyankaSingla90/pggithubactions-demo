name: Demo Workflow

on: 
  pull_request:
    types: [synchronize]
    branches:
    - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - run: echo "Hello! Git Hub actions."
  
